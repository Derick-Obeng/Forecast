{
  "dependencies": {
    "express": "^4.21.2"
  },

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",

      "start": "node index.js",
    "test": "jest",
      "build": "node build.js",
    "build": "next build"


  }


}
